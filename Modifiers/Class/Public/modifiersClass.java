package Modifiers.Class.Public;

public class modifiersClass {
    // Temos o public que vai permitir que essa classe seja acessada de qualquer lugar do projeto.
    // Default é o modificador de acesso padrão, que permite que a classe seja acessada apenas dentro do mesmo pacote.

    // exemplo:

    public static void main(String[] args) {
        System.out.println("O public de acesso de permissao geral");
    }
}
